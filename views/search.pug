extends layout
include utils
append head
  link(rel="stylesheet" href="/stylesheets/index.css")
  script(src="/javascripts/index.js" type="module" defer)

block content
  p(class="homePageTitle") Top Shows
  div(class='showBlock')
    each show in shows
      div(value=show.id)
      div(class="individualShow")
        div(class='titleDiv')
          a(href=`/shows/${show.id}` class="showTitle")= show.title
          div
            a(href=`/shows/${show.id}` )
              img(src=show.imageURL class="showImage")

//- extends layout
//- append head
//- //-   script(src='/javascripts/search.js' type='module')
//- block content
//-     each show in shows
//-       div(value=show.id) 
//-         img(src=show.imageURL)
//-         div
//-           a(href=`/shows/${show.id}`)= show.title
//-           select(name="Ratings" placeholder="Rate")
//-             option 1
//-             option 2
//-             option 3
//-             option 4
//-             option 5
//-           if user
//-             button(type="click" class="ratingSubmit") Rate
//-             select(name="showShelves" class="showShelfSelect") 
//-               each shelf in shelves
//-                 option(value=shelf.id class="showShelfOption" )= shelf.title
//-             button(type="click" class="showShelfSubmit" value=user.id id=show.id) Add to Shelf
    
    